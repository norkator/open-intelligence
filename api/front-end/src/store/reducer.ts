import * as actionTypes from './actionTypes';
import {Reducer} from "redux";

const initialState = {
  selectedDate: null,
};

const reducer = (selectedDate = initialState, action: any): Reducer => {

};

export default reducer;
